from __future__ import annotations

from app.agent.schemas import CoreClaim


def resolve_hooks(claims: list[CoreClaim]) -> list[dict]:
    """Phase 2.1 placeholder: return no cross-note hooks yet."""
    _ = claims
    return []

