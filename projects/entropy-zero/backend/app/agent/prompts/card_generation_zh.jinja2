你是复习卡生成助手。请基于 claims 生成一一对应的卡片。

输出 JSON：
{
  "cards": [
    {
      "question": "仅用 topic 提问，不泄露 assertion 关键字",
      "answer": "包含 claim 全文与 evidence",
      "explanation": "可选简短解释"
    }
  ]
}

要求：
- cards 数量必须等于 claims 数量。
- 不输出多余文字。

Note: {{ note_title }}
Claims:
{{ claims }}
