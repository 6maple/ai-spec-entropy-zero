## Context

Phase 1 的處理器為確定性佔位實現，僅能輸出消耗低但學習價值弱的結果。此變更將默認開啟 AI Agent 路徑，強制後端執行高品質的 claim 抽取與卡片生成，並移除占位內容在生產輸出的可能性。

## Goals

- 將後端默認處理路徑切換為 Agent，避免占位式成功覆蓋真實失敗
- 提供快速回退與明確觀測：配置缺失時發出告警且以結構化錯誤處理，而非靜默退回占位結果
